# Note: numpy, scipy, pandas, matplotlib, seaborn, tqdm, scikit-learn
# are intentionally unpinned to avoid conflicts with Colab's
# pre-installed environment. They are available by default in Colab.

torch>=2.5.0
torchvision>=0.20.0
timm>=1.0.0
umap-learn>=0.5.5
omegaconf>=2.3.0
