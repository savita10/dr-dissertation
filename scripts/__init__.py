"""Standalone scripts for the greyscale (Design C1) ablation.

Runnable with ``python -m scripts.<name>`` from the repo root so ``src`` imports
resolve. Isolated to the ``greyscale-experiment`` ; nothing in the
completed Phase 1-4 pipeline imports from here.
"""
